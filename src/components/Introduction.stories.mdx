import { Meta } from '@storybook/addon-docs'

<Meta title="Components/Introduction" />

# Components

This storybook has only one component - **Button**. It contains different represantations of component and one example of useage.

The storybook was created only for learning purpose.
